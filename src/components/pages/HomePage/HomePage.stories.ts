import HomePage from './HomePage.svelte';

export default {
  component: HomePage,
  title: "HomePage",
};

const Template = () => ({
  Component: HomePage,
});

export const Default = Template.bind({});
